# Python
__pycache__/
*.py[cod]
.venv/
venv/
env/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Notebooks
.ipynb_checkpoints/

# Node / React
node_modules/
dist/
build/
.vite/
*.local

# Env & secrets
.env
.env.*
!.env.example

# OS
.DS_Store
Thumbs.db
desktop.ini

# Claude Code — machine-local permission grants, not shared config
.claude/settings.local.json

# NOTE: data/ is intentionally NOT ignored.
# Generated feature JSON is committed so the React app can consume it and
# so feature-logic changes show up as reviewable diffs.
